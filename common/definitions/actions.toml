# Block result action values.

[actions]
continue = { value = "continue", description = "Pass message to the next block" }
respond  = { value = "respond",  description = "Short-circuit with a response" }
drop     = { value = "drop",     description = "End the chain silently" }
error    = { value = "error",    description = "Short-circuit with an error" }
